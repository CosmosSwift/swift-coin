public enum SupplyKeys {
    // ModuleName is the module name constant used in many places
    public static let moduleName = "supply"

    // StoreKey is the store key string for supply
    public static let storeKey = moduleName

    // RouterKey is the message route for supply
    public static let routerKey = moduleName

    // QuerierRoute is the querier route for supply
    public static let querierRoute = moduleName
}
